# Logs & runtime caches
logs/
*.log
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
node_modules/
dist/
build/
_MEI*/
*.spec

# Frontend build output (bundled into EXE, not committed)
ui_dist/

# Local configurations
.env
.env.enc
.env.bak
.env.secrets
.env*.local
*.db
*.db-journal
*.db-shm
*.db-wal
github_token.txt
secret.key
secret.salt

# Alembic migrations (unused — schema evolves in database.py)
alembic/
alembic.ini

# Runtime output logs
ultron_stdout.txt
ultron_stderr.txt

# Local debug and appdata dumps
decrypted_env.txt
err.txt
out.txt
*.appdata

# Token blacklist (persisted across restarts)
.token_blacklist

# Runtime directories (created on first run)
backups/
reports/
uploads/
client_lock.json

# Frontend boilerplate assets
react.svg
vite.svg
hero.png
# Agent workspace.agents/

# Binary bloat — never commit EXEs
*.exe
server/backend/downloads/


