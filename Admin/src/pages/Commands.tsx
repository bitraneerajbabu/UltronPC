import React from 'react'
/**
 * Commands View
 * TODO: Trigger remote commands (restart, start/stop data collector loop).
 */
export default function Commands() {
  return <div>Remote Commands Pipeline</div>
}