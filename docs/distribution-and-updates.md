# Distribution and Update Protocols

## Packaging
- Uses PyInstaller to bundle Python backend and built UI files into a standalone single-file `UltrON.exe`.
- Utilizes a lightweight bootstrapper (`UltrON_Installer.exe`) that fetches updates from GitHub Releases automatically.\n